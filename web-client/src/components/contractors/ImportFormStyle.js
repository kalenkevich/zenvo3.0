import { createUseStyles } from 'react-jss';

export default createUseStyles(() => ({
  root: {
    display: 'flex',
    margin: '5px 0',
    alignItems: 'center',
  },
  input: {
  },
  button: {
    marginTop: '20px',
    marginLeft: '20px',
  }
}));
